import { useEffect, useRef, useState } from 'react';
import { CalendarDays, CheckCircle2, CheckSquare, Download, FileText, Plus, Send, Upload, X } from 'lucide-react';
import { getApiErrorMessage } from '../../auth/api';
import { downloadPrivateFile, uploadPrivateFile } from '../../files/api';
import { placementsApi } from '../../placements/api';
import type { PlacementRecord } from '../../placements/types';
import { reportsApi } from '../../reports/api';
import type { ReportRecord } from '../../reports/types';

const statusStyle: Record<ReportRecord['status'], string> = { DRAFT: 'bg-slate-100 text-slate-700', SUBMITTED: 'bg-blue-50 text-blue-700', APPROVED: 'bg-emerald-50 text-emerald-700', REJECTED: 'bg-rose-50 text-rose-700' };
const statusLabel: Record<ReportRecord['status'], string> = { DRAFT: 'Bản nháp', SUBMITTED: 'Chờ duyệt', APPROVED: 'Đã duyệt', REJECTED: 'Cần bổ sung' };

export const StudentReports = () => {
  const [reports, setReports] = useState<ReportRecord[]>([]); const [placements, setPlacements] = useState<PlacementRecord[]>([]); const [isOpen, setIsOpen] = useState(false); const [editing, setEditing] = useState<ReportRecord | null>(null); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); const inputRef = useRef<HTMLInputElement>(null);
  const [form, setForm] = useState({ placementId: '', week: 1, title: '', content: '', file: null as File | null });
  const load = async () => { setLoading(true); try { const [reportsPage, placementsPage] = await Promise.all([reportsApi.mine({ limit: 100 }), placementsApi.listMine()]); setReports(reportsPage.items); setPlacements(placementsPage.items.filter((item) => item.status === 'ACTIVE')); } catch (e) { setError(getApiErrorMessage(e)); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, []);
  const openCreate = () => { setEditing(null); setForm({ placementId: '', week: 1, title: '', content: '', file: null }); setIsOpen(true); };
  const openEdit = (report: ReportRecord) => { setEditing(report); setForm({ placementId: report.placementId, week: report.week, title: report.title ?? '', content: report.content, file: null }); setIsOpen(true); };
  const close = () => { if (!saving) { setIsOpen(false); setEditing(null); setError(''); } };
  const submit = async (e: React.FormEvent) => { e.preventDefault(); setSaving(true); setError(''); try { const fileId = form.file ? (await uploadPrivateFile(form.file, 'REPORT')).id : undefined; const report = editing ? await reportsApi.update(editing.id, { title: form.title || undefined, content: form.content, ...(fileId ? { fileId } : {}) }) : await reportsApi.create({ placementId: form.placementId, week: form.week, title: form.title || undefined, content: form.content, fileId }); await reportsApi.submit(report.id); close(); await load(); } catch (e) { setError(getApiErrorMessage(e)); } finally { setSaving(false); } };
  const download = async (fileId: string) => { try { await downloadPrivateFile(fileId); } catch (e) { setError(getApiErrorMessage(e)); } };
  return <div className="space-y-6"><section className="rounded-3xl border border-blue-100 bg-linear-to-br from-blue-50 via-white to-indigo-50 px-6 py-6 shadow-xs sm:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><div className="mb-2 flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-blue-700"><CheckSquare className="h-4 w-4" />Thực tập của tôi</div><h2 className="text-2xl font-extrabold tracking-tight text-slate-900">Báo cáo thực tập tuần</h2><p className="mt-2 text-sm text-slate-600">Ghi nhận tiến độ, đính kèm minh chứng và nhận phản hồi từ giảng viên.</p></div><button disabled={!placements.length} onClick={openCreate} className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white shadow-sm transition hover:bg-blue-700 disabled:cursor-not-allowed disabled:opacity-50"><Plus className="h-4 w-4" />Tạo báo cáo</button></div>{!placements.length && !loading && <p className="mt-4 rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">Bạn cần có kỳ thực tập đang hoạt động và được phân công giảng viên trước khi nộp báo cáo.</p>}</section>{error && <p className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700">{error}</p>}{loading ? <div className="rounded-2xl bg-white p-8 text-sm text-slate-500 shadow-xs">Đang tải báo cáo...</div> : reports.length === 0 ? <div className="rounded-3xl border border-dashed border-slate-300 bg-white px-6 py-14 text-center"><CheckSquare className="mx-auto h-11 w-11 text-slate-300" /><h3 className="mt-4 font-bold text-slate-800">Chưa có báo cáo nào</h3><p className="mt-1 text-sm text-slate-500">Bắt đầu với báo cáo tuần đầu tiên của bạn.</p></div> : <div className="space-y-4">{reports.map((report) => <article key={report.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-xs"><div className="flex flex-col justify-between gap-3 border-b border-slate-100 pb-4 sm:flex-row"><div className="flex gap-3"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-sm font-extrabold text-blue-700">T{report.week}</div><div><h3 className="font-bold text-slate-900">{report.title || `Báo cáo tuần ${report.week}`}</h3><p className="mt-0.5 text-xs text-slate-500">{report.placement.internship.title} · {report.placement.company.companyName}</p></div></div><span className={`self-start rounded-full px-3 py-1 text-xs font-bold ${statusStyle[report.status]}`}>{statusLabel[report.status]}</span></div><p className="mt-4 whitespace-pre-line text-sm leading-6 text-slate-700">{report.content}</p>{report.feedback && <div className="mt-4 rounded-xl border border-violet-100 bg-violet-50 p-3 text-sm text-violet-900"><b>Phản hồi của giảng viên</b><p className="mt-1">{report.feedback}</p></div>}<div className="mt-4 flex flex-wrap gap-2">{report.file && <button onClick={() => void download(report.file!.id)} className="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-3 py-2 text-xs font-bold text-slate-700 hover:bg-slate-50"><Download className="h-4 w-4 text-blue-600" />{report.file.originalName}</button>}{report.status === 'REJECTED' && <button onClick={() => openEdit(report)} className="inline-flex items-center gap-2 rounded-lg bg-amber-500 px-3 py-2 text-xs font-bold text-white hover:bg-amber-600"><CheckCircle2 className="h-4 w-4" />Chỉnh sửa và nộp lại</button>}</div></article>)}</div>}{isOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4 backdrop-blur-sm"><form onSubmit={submit} className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-white shadow-2xl"><header className="flex items-start justify-between border-b border-slate-100 px-6 py-5 sm:px-8"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-blue-700"><FileText className="h-4 w-4" />Báo cáo tiến độ</div><h3 className="mt-1 text-xl font-extrabold text-slate-900">{editing ? `Chỉnh sửa báo cáo tuần ${editing.week}` : 'Tạo báo cáo mới'}</h3><p className="mt-1 text-sm text-slate-500">{editing ? 'Cập nhật theo nhận xét rồi gửi lại cho giảng viên.' : 'Điền nội dung rõ ràng để giảng viên dễ theo dõi.'}</p></div><button type="button" onClick={close} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-700"><X className="h-5 w-5" /></button></header><div className="space-y-5 px-6 py-6 sm:px-8"><div className="grid gap-4 sm:grid-cols-[1fr_150px]"><label className="block"><span className="mb-2 block text-sm font-bold text-slate-800">Kỳ thực tập <span className="text-rose-500">*</span></span><select disabled={!!editing} required value={form.placementId} onChange={(e) => setForm({ ...form, placementId: e.target.value })} className="w-full rounded-xl border border-slate-300 bg-white px-3 py-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100 disabled:bg-slate-100"><option value="">Chọn kỳ thực tập</option>{placements.map((p) => <option key={p.id} value={p.id}>{p.internship.title} — {p.company.companyName}</option>)}</select></label><label className="block"><span className="mb-2 block text-sm font-bold text-slate-800">Tuần báo cáo <span className="text-rose-500">*</span></span><div className="relative"><CalendarDays className="pointer-events-none absolute left-3 top-3.5 h-4 w-4 text-slate-400" /><input disabled={!!editing} required min="1" max="52" type="number" value={form.week} onChange={(e) => setForm({ ...form, week: +e.target.value })} className="w-full rounded-xl border border-slate-300 py-3 pl-9 pr-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100 disabled:bg-slate-100" /></div></label></div><label className="block"><span className="mb-2 block text-sm font-bold text-slate-800">Tiêu đề <span className="font-normal text-slate-400">(không bắt buộc)</span></span><input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} placeholder="Ví dụ: Hoàn thiện module đăng nhập" className="w-full rounded-xl border border-slate-300 px-3 py-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100" /></label><label className="block"><span className="mb-2 block text-sm font-bold text-slate-800">Nội dung báo cáo <span className="text-rose-500">*</span></span><textarea required rows={7} value={form.content} onChange={(e) => setForm({ ...form, content: e.target.value })} placeholder="• Công việc đã hoàn thành&#10;• Kết quả đạt được&#10;• Khó khăn và hướng xử lý&#10;• Kế hoạch tuần tiếp theo" className="w-full resize-y rounded-xl border border-slate-300 px-3 py-3 text-sm leading-6 outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100" /></label><div><span className="mb-2 block text-sm font-bold text-slate-800">Tệp minh chứng <span className="font-normal text-slate-400">(PDF, DOC, DOCX · tối đa 10 MB)</span></span><input ref={inputRef} accept=".pdf,.doc,.docx" type="file" className="hidden" onChange={(e) => setForm({ ...form, file: e.target.files?.[0] ?? null })} /><button type="button" onClick={() => inputRef.current?.click()} className="flex w-full items-center gap-3 rounded-xl border border-dashed border-blue-300 bg-blue-50/60 p-4 text-left hover:bg-blue-50"><span className="rounded-lg bg-white p-2 text-blue-600 shadow-xs"><Upload className="h-5 w-5" /></span><span><b className="block text-sm text-slate-800">{form.file?.name || editing?.file?.originalName || 'Chọn tệp để đính kèm'}</b><span className="text-xs text-slate-500">{form.file ? `${Math.ceil(form.file.size / 1024)} KB` : editing?.file ? 'Giữ tệp hiện tại hoặc chọn tệp mới' : 'Tệp là không bắt buộc'}</span></span></button></div></div><footer className="flex flex-col-reverse gap-3 border-t border-slate-100 bg-slate-50 px-6 py-4 sm:flex-row sm:justify-end sm:px-8"><button type="button" onClick={close} className="rounded-xl px-5 py-3 text-sm font-bold text-slate-600 hover:bg-slate-200">Hủy</button><button disabled={saving} className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white shadow-sm hover:bg-blue-700 disabled:opacity-60"><Send className="h-4 w-4" />{saving ? 'Đang gửi báo cáo...' : editing ? 'Cập nhật và nộp lại' : 'Tạo và nộp báo cáo'}</button></footer></form></div>}</div>;
};
