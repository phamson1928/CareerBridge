import { Award, Building2, GraduationCap } from 'lucide-react';
import type { EvaluationRecord } from '../../evaluations/api';

export function StudentEvaluations({ evaluations }: { evaluations: EvaluationRecord[] }) {
  return <div className="space-y-6"><div className="bg-white rounded-2xl border border-slate-200 p-6"><h2 className="text-xl font-bold text-slate-900">Đánh giá thực tập</h2><p className="mt-1 text-xs text-slate-500">Điểm và nhận xét từ doanh nghiệp, giảng viên hướng dẫn.</p></div>
    {evaluations.length === 0 ? <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center text-sm text-slate-500">Chưa có đánh giá nào cho kỳ thực tập của bạn.</div> : <div className="space-y-4">{evaluations.map((evaluation) => <article key={evaluation.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-xs"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2 text-sm font-bold text-slate-900">{evaluation.type === 'COMPANY' ? <Building2 className="h-4 w-4 text-emerald-600" /> : <GraduationCap className="h-4 w-4 text-violet-600" />}{evaluation.type === 'COMPANY' ? 'Đánh giá từ doanh nghiệp' : 'Đánh giá từ giảng viên'}</div><p className="mt-1 text-xs text-slate-500">{evaluation.placement.company.companyName} · {evaluation.placement.internship.title}</p></div><span className="shrink-0 rounded-xl bg-emerald-50 px-3 py-2 text-sm font-black text-emerald-700"><Award className="mr-1 inline h-4 w-4" />{evaluation.score}/10</span></div><div className="mt-4 rounded-xl bg-slate-50 p-4 text-sm leading-6 text-slate-700">{evaluation.comment || 'Chưa có nhận xét chi tiết.'}</div><time className="mt-3 block text-xs text-slate-400">Cập nhật: {new Date(evaluation.updatedAt).toLocaleString('vi-VN')}</time></article>)}</div>}
  </div>;
}
