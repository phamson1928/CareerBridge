import React, { useEffect, useState } from 'react';
import { AlertCircle, BriefcaseBusiness, Building2, CalendarDays, CheckCircle2, Clock3, GraduationCap, LoaderCircle, MapPin } from 'lucide-react';
import { getApiErrorMessage } from '../../auth/api';
import { placementsApi } from '../../placements/api';
import type { PlacementRecord } from '../../placements/types';

function formatDate(value: string | null) {
  return value ? new Intl.DateTimeFormat('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' }).format(new Date(value)) : 'Chưa cập nhật';
}

export const PlacementOverview: React.FC = () => {
  const [placements, setPlacements] = useState<PlacementRecord[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    void placementsApi.listMine().then((result) => setPlacements(result.items)).catch((requestError) => setError(getApiErrorMessage(requestError))).finally(() => setLoading(false));
  }, []);

  if (loading) return <div className="flex min-h-72 items-center justify-center text-sm text-slate-500"><LoaderCircle className="mr-2 h-5 w-5 animate-spin" />Đang tải thông tin thực tập...</div>;
  if (error) return <div className="rounded-2xl border border-rose-200 bg-rose-50 p-5 text-sm text-rose-700"><AlertCircle className="mr-2 inline h-4 w-4" />{error}</div>;

  return <section className="space-y-5"><div className="rounded-3xl bg-gradient-to-br from-emerald-950 via-emerald-900 to-slate-900 p-6 text-white shadow-lg sm:p-8"><div className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1 text-[11px] font-bold uppercase tracking-wider text-emerald-100"><BriefcaseBusiness className="h-3.5 w-3.5" /> Hành trình thực tập</div><h1 className="mt-4 text-2xl font-black tracking-tight sm:text-3xl">Vị trí thực tập của tôi</h1><p className="mt-2 max-w-xl text-sm leading-6 text-emerald-100/80">Theo dõi vị trí đã được xác nhận, thời gian thực tập và giảng viên hướng dẫn.</p></div>
    {placements.length === 0 ? <div className="rounded-2xl border border-dashed border-slate-300 bg-white px-6 py-16 text-center shadow-xs"><BriefcaseBusiness className="mx-auto h-10 w-10 text-slate-300" /><h2 className="mt-4 text-base font-black text-slate-800">Chưa có placement</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">Sau khi doanh nghiệp chấp nhận đơn ứng tuyển, thông tin thực tập sẽ được hiển thị tại đây.</p></div> : <div className="grid gap-5 lg:grid-cols-2">{placements.map((placement) => <article key={placement.id} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-xs"><div className={`h-1.5 ${placement.status === 'ACTIVE' ? 'bg-emerald-500' : placement.status === 'PENDING' ? 'bg-amber-400' : 'bg-slate-400'}`} /><div className="p-5 sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-widest text-slate-400">{placement.semester.name}</p><h2 className="mt-1 text-lg font-black text-slate-900">{placement.internship.title}</h2></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold ${placement.status === 'ACTIVE' ? 'bg-emerald-50 text-emerald-700' : placement.status === 'PENDING' ? 'bg-amber-50 text-amber-700' : 'bg-slate-100 text-slate-600'}`}>{placement.status === 'ACTIVE' ? 'Đang thực tập' : placement.status === 'PENDING' ? 'Chờ phân công' : placement.status}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="flex items-center gap-2 text-xs text-slate-600"><Building2 className="h-4 w-4 text-indigo-500" />{placement.company.companyName}</div><div className="flex items-center gap-2 text-xs text-slate-600"><MapPin className="h-4 w-4 text-indigo-500" />{placement.internship.location ?? 'Chưa cập nhật địa điểm'}</div><div className="flex items-center gap-2 text-xs text-slate-600"><CalendarDays className="h-4 w-4 text-indigo-500" />{formatDate(placement.startDate)} — {formatDate(placement.endDate)}</div><div className="flex items-center gap-2 text-xs text-slate-600"><Clock3 className="h-4 w-4 text-indigo-500" />{placement.internship.workType ?? 'Hình thức chưa cập nhật'}</div></div><div className="mt-6 rounded-2xl border border-slate-200 bg-slate-50 p-4">{placement.supervision ? <div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-100 text-indigo-700"><GraduationCap className="h-5 w-5" /></div><div><p className="text-[10px] font-black uppercase tracking-wider text-slate-400">Giảng viên hướng dẫn</p><p className="mt-1 text-sm font-black text-slate-900">{placement.supervision.lecturer.fullName}</p><p className="mt-0.5 text-[11px] text-slate-500">{placement.supervision.lecturer.title ?? 'Giảng viên'} · {placement.supervision.lecturer.department}</p></div><CheckCircle2 className="ml-auto h-5 w-5 text-emerald-500" /></div> : <div className="flex items-start gap-3"><Clock3 className="mt-0.5 h-5 w-5 text-amber-500" /><div><p className="text-sm font-black text-slate-800">Đang chờ nhà trường phân công</p><p className="mt-1 text-xs leading-5 text-slate-500">Bạn sẽ thấy thông tin giảng viên ngay sau khi Admin hoàn tất phân công.</p></div></div>}</div></div></article>)}</div>}
  </section>;
};
